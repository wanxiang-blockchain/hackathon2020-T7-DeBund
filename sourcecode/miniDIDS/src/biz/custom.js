import apiConfig from './apiConfig';
import bizFuns from './bizFuns';
import codeConfig from './codeConfig';
import imgUrl from './image';

const custom = {
    imgUrl,
    bizFuns,
    apiConfig,
    codeConfig,
};

export default custom;
exports.module = custom;
